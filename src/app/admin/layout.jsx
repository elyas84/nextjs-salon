export const metadata = {
  title: "Admin",
  description: "Salon management — inventory, bookings, orders, and content.",
};

export default function AdminLayout({ children }) {
  return children;
}
