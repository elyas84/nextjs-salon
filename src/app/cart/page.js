import CartPageClient from "@/components/store/cart-page-client";

export const metadata = {
  title: "Cart",
  description: "Review salon retail items and continue to checkout.",
};

export default function CartPage() {
  return <CartPageClient />;
}
